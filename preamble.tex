% Set Beamer Theme (Madrid as default)
\usetheme{Madrid}

% Font Overrides
\usepackage{fontspec}
\setmainfont{MesloLGL Nerd Font}
\setsansfont{TeX Gyre Heros}
\setmonofont{Courier}

% Comment out Widescreen Geometry (Beamer Will Manage It)
%\usepackage[a4paper,landscape,margin=1in]{geometry}

% Remove Libertinus Fallbacks (Removed to avoid clashes during override)
\renewcommand{\rmfamily}{\setmainfont{MesloLGL Nerd Font}}
\renewcommand{\sffamily}{\setsansfont{TeX Gyre Heros}}
\renewcommand{\ttfamily}{\setmonofont{Courier}}

% Math Font
\setmathfont{Courier}[Scale=MatchLowercase]
\usepackage[no-math]{fontspec}